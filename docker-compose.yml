services:
  api: 
    build: . 
    command: uvicorn app:app --host 0.0.0.0 --port 8000
    env_file: .env 
    ports: ["8000:8000"]

  scheduler: 
    build: . 
    command: python -m scheduler.sch 
    env_file: .env 
    
    